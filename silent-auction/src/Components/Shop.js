import React from "react";

const Shop = () => {
  //all current auction and remove all auction that are at 00:00
  //only show timer and picture of item here with .get and prop drill down to card level
  return (
    <div>
      <p>shop.js</p>
    </div>
  );
};

export default Shop;
